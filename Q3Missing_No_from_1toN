def missingNumber( A, N):
    sum = 0
    for i in range(N-1):
         sum += A[i]

    exp_sum = N*(N+1)//2     
    return (exp_sum - sum)


#example use
result = missingNumber([1,2,4], 4)
print(result)